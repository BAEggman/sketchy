# s18p03 金 재상(팔) · s18p06 土 농부(다리) — 작업기록 2026-08-08

대화창: 재상 `app/cfa0265e734895ec` (8판) · 농부 `app/285c6e49ee923e76` (3판)

## Gemini 대화창 운용 — 확정
1. `__INS(txt)` = `execCommand('selectAll')` + `execCommand('insertText')`
2. **Enter는 끝내 안 먹는다.** 입력창 안 진짜 클릭 뒤에도 안 보내진다.
3. **보내기 버튼 진짜 클릭이 유일한 방법이고, 첫 클릭은 무시되고 두 번째에 보내진다.**
4. **한 판을 받으면 UI가 「대답 생성 중지」로 굳는다.** 다음 판을 보내려면 **매번 대화창 URL로 새로고침**.
5. 순서 고정: **새로고침 → __INS → 보내기 클릭 ×2 (필요하면 화면 좌표 재확인 후 한 번 더)**

## s18p03 金 재상 — 8판

| 판 | 고친 것 | 남은 것 |
|---|---|---|
| v1 | 첫 생성 | 두 손 다 손등 · 약지에서 시작 · 어깨에서 끊김 · 입 다뭄 |
| v2 | 입 벌림·이 보임 | **허리 가로 고리** · 사발 사라짐 · 발 잘림 |
| v3 | 고리 제거 · 사발 복귀 · 발 회복 | 두 줄이 입에서 만남 |
| v4 | 두 줄이 각자 반쪽에 머묾 | 긴 줄이 입에서 끝남 |
| v5 | **긴 줄이 코 옆까지 · 아랫니 곁가지** | 짧은 줄 끝점 없음 · 배의 샘 따로 놈 |
| v6 | **소용돌이가 줄의 굽이가 됨 · 빗장뼈 아래 점** | 짧은 줄이 다시 입까지 올라가 긴 줄과 붙음 |
| v7 | 물줄기 색이 청록으로 또렷해짐 | 짧은 줄이 여전히 목까지 올라감 |
| **v8** | **「소용돌이 위쪽을 지워라」로 짧은 줄이 소용돌이에서 멈춤** | **발·사발이 판면 아래로 잘림(화각)** |

**배운 것:** 「이렇게 그려라」보다 **「이 부분을 지워라」**가 훨씬 잘 먹는다.
그리고 **편집이 통째 재생성으로 튀어 앞 판의 요소가 사라진다** — 매 판마다 **잘된 것을 이름으로 먼저 못박고** 고칠 것만 나열해야 한다.

## s18p06 土 농부 — 3판

| 판 | 고친 것 | 남은 것 |
|---|---|---|
| v1 | 첫 생성 — 농부·삿갓·노란 옷·벌린 입·비껴선 자세 다 맞음 | 발 잘림 · 사발 없음 · 물줄기가 옷 위 · 소용돌이가 가슴 |
| v2 | **바지를 무릎 위로 걷어 맨살에 물줄기 · 소용돌이 배로 내림** | 발 잘림 · 사발 없음 · **입술 두름(環脣) 없음** |
| v3 | (진행 중 — 「사람을 훨씬 작게, 위아래 여백」) | — |

## 다음
1. **두 칸 다 화각(발·사발이 잘림)** — 「사람을 판면 높이의 2/3로, 발 아래 빈 땅」이 공통 처방
2. 통과하면 **나란히 놓고 역방향 판독** — 「왼쪽이 陰, 오른쪽이 陽으로 읽히는가」
3. 기계 검산(살색 기준선 · 물줄기 색 · 가로 두름 0) → 원본 1024px 내려받기 → `confirm.py` → library.json
